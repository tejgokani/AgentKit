Analyze the cloud usage hotspots below. For each hotspot, return exactly one diagnosis and one recommendation, keyed by hotspotId (use the hotspot's `id`). Do not add, drop, or merge hotspots.

Billing period: {{triggerNode_1.output.periodLabel}}

Hotspots (JSON, one per line):
{{triggerNode_1.output.hotspots}}
