Diagnose the dominant carbon driver for each hotspot below. Return one result
per hotspot in a `diagnoses` array, each keyed by `hotspotId` set to that
hotspot's `id`. Do not add, drop, or merge hotspots.

Billing period: {{triggerNode_1.output.periodLabel}}

Hotspots (JSON, one object per line):
{{triggerNode_1.output.hotspots}}
